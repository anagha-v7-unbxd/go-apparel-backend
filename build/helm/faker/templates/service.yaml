apiVersion: v1
kind: Service
metadata:
  name: {{ include "faker.fullname" . }}
  labels:
    app: {{ include "faker.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "faker.name" . }}
